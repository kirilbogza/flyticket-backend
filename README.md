## flyticket_backend

```markdown
Flight provider backend API: Admins can create flights with timestamps, cities, and seat counts.Third-party providers can query tickets by date and cities, receiving exact departure and arrival times with pricing.
```